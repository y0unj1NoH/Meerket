import { StickerWrapper } from "./styled";
import { Logo } from "components/atoms";

/**
 * 링크 아이콘 ( 거래희망장소 `>` )
 */
export const Sticker = () => {
  return (
    <StickerWrapper>
      <Logo />
    </StickerWrapper>
  );
};
