import styled from "@emotion/styled";

export const RoundImageWithTextWrapper: ReturnType<typeof styled.div> =
  styled.div`
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
    cursor: pointer;
  `;
